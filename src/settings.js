// eslint-disable-next-line import/prefer-default-export
export const Settings = {
  // points settings
  POINTS_COUNT: 1000,
  POINTS_SPREAD: 500,

  // area for looking points to connect
  NEAR_BORDER: 200,
  FAR_BORDER: 300,

  // camera clipping
  CLIPPING_NEAR: 1,
  CLIPPING_FAR: 10000,

  // links
  LINK_SEGMENT_LENGTH: 30,
  LINK_STEP_TIME: 1500,
  LINK_LINE_WIDTH: 3,

  // link's segments
  SEGMENT_STATE_COUNT: 5, // equals to length of visible segments
  SEGMENT_MIN_DISTORTION: 10,
  SEGMENT_MAX_DISTORTION: 20,

};
